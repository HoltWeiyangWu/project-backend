spring:
  application:
    name: backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/project
    username: root
    password: wuweiyang
server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: true